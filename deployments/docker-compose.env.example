# =============================================================================
# Agent OS — Docker Compose Environment File Template
# =============================================================================
# Copy to .env (on the deploy host, alongside docker-compose.yml) and fill in.
# Lines with actual values are gitignored — DO NOT commit real secrets.
# =============================================================================

# --- Database ---
POSTGRES_DB=agentos
POSTGRES_USER=agentos
POSTGRES_PASSWORD=agentos_dev

# --- Web ---
# Host port to expose the web frontend on. 8080 by default.
WEB_PORT=8080

# --- API config ---
LITELLM_URL=http://xps.taila4541b.ts.net:4000
XAI_API_KEY=
HERMES_API_KEY=
OPENROUTER_API_KEY=
GEMINI_API_KEY=
FAL_KEY=
ZAI_API_KEY=
AOS_MASTER_KEY=
AOS_DEV_LOGIN=tim
ANTHROPIC_API_KEY=
OPENAI_API_KEY=
OMI_API_TOKEN=
OMI_BASE_URL=
LLM_MODEL=local-qwen
